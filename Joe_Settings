# find all processes with 'shotgun' in the name and kill them. Start up shotgun 
sg()
{
  ps -A | grep -E ".*shotgun.*" | xargs kill
  shotgun
}

# Drop, Create, Migrate, and Seed your Sinatra project
reset()
{
  bundle exec rake db:drop
  bundle exec rake db:create
  bundle exec rake db:migrate
  bundle exec rake db:seed
}

# download and setup basic sinatra skeleton
sinatra()
{
  wget "http://cl.ly/2n3D2V0R0L2f/download/sinatra_skeleton.zip"
  unzip sinatra_skeleton.zip -d ./$@
  rm sinatra_skeleton.zip
  cd ./$@
  git init
  git add .
  git commit -am "sinatra skeleton --b"
}

# search all files recursively starting from $2 and search for string $1. Case insensative
fws()
{
  grep -rEil $1 $2
}

alias joe="cd ~/Dropbox/DBC-Chicago/Grasshoppers/JNoonan/"

# current side project
alias cs="cd /Users/apprentice/Dropbox/DBC-Chicago/Grasshoppers/JNoonan/Phase2/Grasshopper/PracticingActiveRecord" 

alias c=""
# make ls more intelligible
alias ls='ls -FGh'

# clear terminal history
alias mm='history -c'

# check out bash_profile
alias bp="subl ~/.bash_profile"

joe_skeleton()
{
   git clone https://github.com/Joe11000/Project_Skeleton-User-Auth---Layout- $1
   git remote rm
   git checkout -b Attempt1
   git commit -m "Project Setup Initial Commit"
}

# install sass gem if doesn't exist
gem query --remote | grep -Eqi
if [ $? -eq 0 ]; then
  gem install sass &
fi


